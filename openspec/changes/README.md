# Active Changes Directory

Each change uses:
- `openspec/changes/<change-id>/proposal.md`
- `openspec/changes/<change-id>/tasks.md`
- `openspec/changes/<change-id>/specs/<domain>/spec.md` (one or more delta specs)

Change ID format: `YYYYMMDD-<kebab-scope>`.
