# OpenSpec Workspace

This directory contains the canonical spec and change-tracking artifacts for this repository.

- `specs/`: current accepted specifications by domain
- `changes/`: active change proposals and task progress
- `changes/archive/`: completed and archived changes

See:
- `project.md` for repository conventions
- `AGENTS.md` for strict execution rules
