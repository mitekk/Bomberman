# Auto-load local secrets for Codex and related tooling.
# Requires direnv: https://direnv.net/
if command -v dotenv_if_exists >/dev/null 2>&1; then
  dotenv_if_exists
elif [ -f .env ]; then
  set -a
  . ./.env
  set +a
fi
